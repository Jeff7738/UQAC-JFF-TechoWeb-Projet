# import pytest

# class TestProduct(object):
#     def test_product(self, app, client):
#         with app.app_context():
#             response = client.get("/")
#             assert response.status_code == 200
#             assert b"Brown eggs" in response.data